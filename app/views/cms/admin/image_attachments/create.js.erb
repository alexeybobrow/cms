var attachments_cache = $('#attachments_cache');
attachments_cache.val(attachments_cache.val() + ',' + "<%= @attachment.id %>");

$('.files').append(
  '<%= escape_javascript render(partial: "cms/admin/shared/attachment", locals: { attachment: @attachment, content: @content }) %>'
);
