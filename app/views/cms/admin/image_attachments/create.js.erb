var attachments_cache = $('#attachments_cache');
attachments_cache.val(attachments_cache.val() + ',' + "<%= @upload.id %>");

$('.files').append(
  '<%= escape_javascript render(partial: "cms/admin/shared/attachment", locals: { attachment: @upload, attachments: @attachments }) %>'
);
